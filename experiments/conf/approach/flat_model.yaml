defaults:
  - ours

# Flat Beta-Bernoulli model: independent posterior per (human, layout, subtask).
# No hierarchy across layouts or humans. Same max-entropy exploration.
# Ablates Claims 2 & 3 (cross-layout and cross-human transfer).
preference_model_type: flat
explore_method: max-entropy
mood_learning_enabled: false
